apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-svc
  namespace: openchat-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: post-svc
  template:
    metadata:
      labels:
        app: post-svc
    spec:
      containers:
        - name: post-svc
          image: openchat/post-svc:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: openchat-config
          readinessProbe:
            httpGet:
              path: /ready
              port: 8081
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: post-svc
  namespace: openchat-dev
spec:
  selector:
    app: post-svc
  ports:
    - name: http
      port: 80
      targetPort: 8081
